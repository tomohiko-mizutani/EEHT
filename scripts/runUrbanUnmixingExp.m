clear;
opts.displayFlag = 0;

preProcOpt = 1;
urbanUnmixingExp(preProcOpt, opts);

preProcOpt = 2;
urbanUnmixingExp(preProcOpt, opts);

preProcOpt = 3;
urbanUnmixingExp(preProcOpt, opts);

preProcOpt = 4;
urbanUnmixingExp(preProcOpt, opts);

preProcOpt = 5;
urbanUnmixingExp(preProcOpt, opts);
